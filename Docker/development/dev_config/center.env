# CENTER_CONFIG_PATH: runtime config path inside the container.
# Unset (or empty) → uses the default /app/config.yml baked into the image.
# Override example:
#   CENTER_CONFIG_PATH=/app/config/config.dev.yml
