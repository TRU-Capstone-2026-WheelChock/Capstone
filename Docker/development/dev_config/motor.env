# MOTOR_CONFIG_PATH: runtime config path inside the container.
# Use the compose-friendly config so the motor subscribes to the `center` service.
MOTOR_CONFIG_PATH=/app/config/config.compose.yml
# MOTOR_CONFIG_PATH=/app/config/config.yml
