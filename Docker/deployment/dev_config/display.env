# DISPLAY_CONFIG_PATH: runtime config path inside the container.
# Use the compose-friendly config so the subscriber connects to the `center` service.
DISPLAY_CONFIG_PATH=/app/config/config.compose.yml
