# Host-side ports for services included from the root development stack.
# Child compose files still keep `:-default` fallbacks so they can run locally.
DISPLAY_HTTP_PORT=8080
THERMAL_VISUALIZER_PORT=8000
